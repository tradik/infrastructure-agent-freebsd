version: '3.1'

services:
  minimal-agent:
    build:
      context: ../../
      dockerfile: test/proxy/Dockerfile_agent
    image: minimal-agent
    volumes:
      - ./agent:/etc/newrelic-infra
    ports:
      - "4445:4445"
    command: ["/agentservice"]
